[package]
name = "engine-macros"
version = "0.1.0"
edition = "2021"
authors = ["Agent Game Engine Team"]

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"

[dev-dependencies]
trybuild = "1.0"
