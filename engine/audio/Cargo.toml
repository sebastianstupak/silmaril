[package]
name = "engine-audio"
version = "0.1.0"
edition = "2021"
authors = ["Agent Game Engine Team"]

[dependencies]
kira = "0.8"
engine-core = { path = "../core" }
glam = "0.25"

[dev-dependencies]
criterion = "0.5"
