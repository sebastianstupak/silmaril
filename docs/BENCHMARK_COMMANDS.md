# Benchmark Commands Reference

**Complete guide to all benchmark and test commands**

---

## 🚀 Quick Start

### Industry Comparison

```bash
# Show quick threshold reference in terminal
just benchmark-thresholds

# Generate full comparison report (auto-parses results!)
just benchmark-report

# View the generated report
cat BENCHMARK_COMPARISON.md
```

---

## 📊 Run Benchmarks by Feature

```bash
# ECS (Entity Component System)
just benchmark-ecs

# Serialization
just benchmark-serialization

# Physics
just benchmark-physics

# Renderer
just benchmark-renderer

# Math operations
just benchmark-math

# Allocators
just benchmark-allocators

# Spatial data structures
just benchmark-spatial

# Profiling overhead
just benchmark-profiling

# Asset loading
just benchmark-assets

# Platform-specific
just benchmark-platform

# Network (when implemented)
just benchmark-network
```

---

## 🧪 Run Tests by Feature

```bash
# ECS tests
just test-ecs

# Serialization tests
just test-serialization

# Physics tests
just test-physics

# Renderer tests
just test-renderer

# Math tests
just test-math

# Networking tests
just test-networking

# Profiling tests
just test-profiling

# All tests
just test

# Verbose output
just test-verbose

# Client code only
just test-client

# Server code only
just test-server
```

---

## 🎯 Run All Benchmarks

```bash
# Run all benchmarks (comprehensive)
just benchmark-all

# Quick smoke test (fast, for CI)
just benchmark-smoke

# Dev benchmarks (fast iteration)
just dev-benchmark

# With profiling enabled
just benchmark-profile

# All platforms
just benchmark-all-platforms
```

---

## 📈 Baseline & Regression Testing

```bash
# Save current performance as baseline
just benchmark-save-baseline

# Update specific baseline
just benchmark-update-baseline baseline_name="my-baseline"

# Compare with saved baseline
just benchmark-compare-baseline

# Compare with threshold (20% regression fails)
just benchmark-compare-baseline baseline_name="main" threshold="20"
```

---

## 🏆 Industry Comparison Commands

### Quick Reference (Terminal)
```bash
just benchmark-thresholds
```

**Output:**
```
=== Industry Performance Thresholds ====

FRAME TIME:
  60 FPS:  16.67ms (Unreal/AAA)
  120 FPS:  8.33ms (Competitive/VR)

ECS PERFORMANCE:
  agent-game-engine: 10M entities/sec (target)
  Unity DOTS:        2.25M entities/sec
  Bevy v0.16:        3x improvement

SERIALIZATION (1000 entities):
  agent-game-engine: 99.3μs (50x faster than 5ms target!)
  bincode:           2962 MB/s serialize
...
```

### Full Comparison Report (Auto-Generated!)
```bash
just benchmark-report
```

**What it does:**
1. ✅ Parses actual Criterion benchmark results
2. ✅ Extracts real performance numbers
3. ✅ Calculates comparisons vs Unity DOTS
4. ✅ Generates `BENCHMARK_COMPARISON.md` automatically

**Example Output:**
```
Parsing benchmark results from: target/criterion
  ECS Iteration: 19.6M/s
  ECS Spawning: 241.9M/s
  ECS Multi-Component: 10.1M/s
[OK] Report saved to: BENCHMARK_COMPARISON.md
```

### View Report in Browser
```bash
just benchmark-view
```
Opens the Criterion HTML report in your browser.

---

## 📋 Typical Workflows

### Daily Development

```bash
# Quick test
just test-ecs

# Quick benchmark
just benchmark-smoke

# Check thresholds
just benchmark-thresholds
```

### Before Commit

```bash
# Run all tests
just test

# Run all benchmarks
just benchmark-all

# Generate comparison report
just benchmark-report

# Compare with baseline (fail if >20% regression)
just benchmark-compare-baseline threshold="20"
```

### Performance Investigation

```bash
# Run specific feature benchmark
just benchmark-ecs

# Run with profiling
just benchmark-profile

# Check thresholds
just benchmark-thresholds

# Generate full report
just benchmark-report

# View detailed results
just benchmark-view
```

### Release Preparation

```bash
# Save new baseline
just benchmark-save-baseline

# Run comprehensive benchmarks
just benchmark-all-platforms

# Generate comparison report
just benchmark-report

# Review results
cat BENCHMARK_COMPARISON.md
just benchmark-view
```

---

## 📊 Output Locations

### Criterion Reports
```
target/criterion/
├── report/
│   └── index.html          # Open with: just benchmark-view
├── ecs_comprehensive/
├── serialization/
└── ...
```

### Industry Comparison
```
BENCHMARK_COMPARISON.md      # Generated by: just benchmark-report
benchmark_thresholds.yaml    # Industry data source
```

### Test Results
```
Standard cargo test output
Use --nocapture for verbose: just test-verbose
```

---

## 🎯 Data Sources

All industry thresholds sourced from:
- **Unity DOTS:** https://medium.com/superstringtheory/unity-dots-ecs-performance-amazing-5a62fece23d4
- **Unreal Nanite:** https://dev.epicgames.com/documentation/en-us/unreal-engine/nanite-virtualized-geometry-in-unreal-engine
- **Bevy Engine:** https://bevyengine.org/
- **Rapier Physics:** https://dimforge.com/blog/2020/08/25/announcing-the-rapier-physics-engine/
- **And more...**

**Complete data:** `benchmark_thresholds.yaml` (488 lines)

---

## 🔧 Command Reference

### Benchmark Commands

| Command | Description |
|---------|-------------|
| `benchmark-ecs` | Run ECS benchmarks |
| `benchmark-serialization` | Run serialization benchmarks |
| `benchmark-physics` | Run physics benchmarks |
| `benchmark-renderer` | Run renderer benchmarks |
| `benchmark-math` | Run math benchmarks |
| `benchmark-allocators` | Run allocator benchmarks |
| `benchmark-spatial` | Run spatial structure benchmarks |
| `benchmark-profiling` | Run profiling overhead benchmarks |
| `benchmark-all` | Run all benchmarks |
| `benchmark-smoke` | Quick smoke test |
| `benchmark-thresholds` | Show industry thresholds |
| `benchmark-report` | Generate comparison report |
| `benchmark-view` | Open report in browser |

### Test Commands

| Command | Description |
|---------|-------------|
| `test-ecs` | Test ECS |
| `test-serialization` | Test serialization |
| `test-physics` | Test physics |
| `test-renderer` | Test renderer |
| `test-math` | Test math |
| `test-networking` | Test networking |
| `test-profiling` | Test profiling |
| `test` | Run all tests |
| `test-verbose` | Verbose test output |
| `test-client` | Client tests only |
| `test-server` | Server tests only |

---

## 🚨 Important Notes

### Automatic Report Generation

The `benchmark-report` command now automatically parses Criterion results!

**No manual editing needed:**
- ✅ Reads JSON data from `target/criterion/`
- ✅ Calculates throughput automatically
- ✅ Compares vs industry standards
- ✅ Updates every time you run it

**Workflow:**
```bash
just benchmark-ecs          # Run benchmarks
just benchmark-report       # Auto-parse results
cat BENCHMARK_COMPARISON.md # View comparison
```

### Command Naming Convention

All commands use dash syntax:
- ✅ `benchmark-ecs` (correct)
- ❌ `bench-ecs` (old, removed)
- ❌ `benchmark:ecs` (not supported by justfile)

### Windows Compatibility

All commands work on Windows!
- Uses PowerShell for scripts
- Automatic parsing works cross-platform
- UTF-8 encoding handled correctly

---

## 📚 Related Documentation

- [BENCHMARKING_SYSTEM_COMPLETE.md](../BENCHMARKING_SYSTEM_COMPLETE.md) - Implementation details
- [BENCHMARK_COMPARISON.md](../BENCHMARK_COMPARISON.md) - Latest comparison report
- [benchmark_thresholds.yaml](../benchmark_thresholds.yaml) - Industry data
- [ECS_BENCHMARK_RESULTS.md](../ECS_BENCHMARK_RESULTS.md) - Detailed ECS results

---

*Last Updated: 2026-02-02*
*Commands: All using dash syntax (`benchmark-feature`, `test-feature`)*
