[package]
name = "moba-client"
version.workspace = true
edition.workspace = true

[dependencies]
# silmaril = { path = "../../../" }
moba-shared = { path = "../shared" }
serde.workspace = true
tokio.workspace = true

# Add client-specific dependencies
# bevy or similar for rendering
