[package]
name = "moba-server"
version.workspace = true
edition.workspace = true

[dependencies]
# silmaril = { path = "../../../" }
moba-shared = { path = "../shared" }
serde.workspace = true
tokio.workspace = true

# Add server-specific dependencies
# sqlx for database access
# redis for matchmaking/caching
