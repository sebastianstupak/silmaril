[package]
name = "moba-shared"
version.workspace = true
edition.workspace = true

[dependencies]
# silmaril = { path = "../../../" }
serde.workspace = true

# Add shared dependencies for heroes, abilities, protocol, etc.
